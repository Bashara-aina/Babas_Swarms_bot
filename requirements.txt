# Legion Swarms Bot — Python dependencies
# Install: pip install -r requirements.txt

# ── Core Telegram bot ─────────────────────────────────────────────────────
aiogram>=3.4.1
python-dotenv>=1.0.0

# ── LLM client ────────────────────────────────────────────────────────────
litellm>=1.35.0

# ── Database ──────────────────────────────────────────────────────────────
aiosqlite>=0.19.0

# ── Context database (OpenViking — L0/L1/L2 tiered memory) ───────────────
# pip install openviking  (Python 3.9+, no server needed, local file store)
openviking>=0.2.5

# ── File I/O ──────────────────────────────────────────────────────────────
aiofiles>=23.0.0
Pillow>=10.0.0

# ── Computer use / screen tools ───────────────────────────────────────────
pyscreenshot>=0.4.2
pyautogui>=0.9.54
xdotool  # system package: sudo apt install xdotool

# ── System monitoring (resource_monitor) ─────────────────────────────────
psutil>=5.9.0
# pynvml  — install if you have an NVIDIA GPU:
# pip install pynvml

# ── Research / web tools ─────────────────────────────────────────────────
httpx>=0.27.0
beautifulsoup4>=4.12.0
requests>=2.31.0

# ── Document tools ────────────────────────────────────────────────────────
pymupdf>=1.23.0        # PDF
openpyxl>=3.1.0        # Excel
python-docx>=1.1.0     # Word
pdfminer.six>=20221105

# ── Optional: Obsidian export, Markdown ──────────────────────────────────
# markdown>=3.5.0
