# Legion Swarms Bot — Python dependencies
# Install: python -m pip install -r requirements.txt

# Core bot
aiogram>=3.4.1,<4.0.0
python-dotenv>=1.0.0
litellm>=1.57.0

# Async + HTTP
aiohttp>=3.9.0
httpx>=0.27.0
aiofiles>=23.2.0

# Persistence / system
aiosqlite>=0.20.0
psutil>=5.9.0

# Context memory (optional but supported)
openviking>=0.2.5
chromadb==1.5.6
aiofiles>=23.2.0
requests>=2.31.0
beautifulsoup4>=4.12.0
playwright>=1.40.0
feedparser>=6.0.0
arxiv>=2.1.0

# Documents / OCR
Pillow>=10.0.0
openpyxl>=3.1.0
pdfplumber>=0.10.0
pytesseract>=0.3.10
python-docx>=1.1.0
PyMuPDF>=1.23.0
pdfminer.six>=20221105

# Email
aioimaplib>=1.1.0
aiosmtplib>=3.0.0

# Optional desktop libs used by compatibility modules
pyautogui>=0.9.54
pyscreenshot>=3.1

# v5 integrations
openai-agents==0.4.2
camel-ai==0.2.89
ag2==0.11.4
smolagents==1.23.0
pydantic-ai-slim==1.61.0
agentops==0.3.26
networkx>=3.0
knowledge-graph>=0.0.33
swarms==9.0.4
pytest-asyncio>=0.23.0

# === v6 Humanization Layer ===
mem0ai==1.0.11
chromadb==1.5.6
faster-whisper==1.2.1
kokoro-onnx==0.5.0
soundfile==0.13.1
scipy==1.16.2
letta-client==1.10.2
graphiti-core==0.28.2
memobase>=0.0.27
sentence-transformers==5.2.3
open-interpreter==0.4.3

# === v7 Tier 1 — Soul Transplant + Capability Expansion ===

# Composio — 850+ tool connectors (Gmail, Calendar, WhatsApp, GitHub, Notion...)
composio-core>=0.7.0
composio-langchain>=0.7.0

# CrewAI — multi-agent business crew (Rumahlabuh management)
crewai>=0.80.0

# Google auth (for direct Calendar/Gmail if not using Composio)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.127.0

# Supabase Python client (upgrade to latest)
supabase>=2.4.0

# LangChain core (for autonomous router ReAct loop + Composio integration)
langchain-core>=0.3.0
langchain>=0.3.0
langchain-community>=0.3.0

# === v8 Tier-3 — Screenpipe, MCP, LiveKit token helpers, search ===
mcp>=1.2.0
livekit-api>=0.8.0
duckduckgo-search>=4.0.0
apscheduler>=3.10.0

# === v9 Soul Transplant ===
browser-use==0.1.40  # autonomous LLM-driven browsing (browse_task)
pytz>=2024.1  # timezone support (fallback to stdlib where possible)

# === Phase 1 ClawCode Upgrade ===
yt-dlp>=2026.1.0  # video/audio download (ffmpeg-backed)
crawl4ai>=0.4.0  # AI-friendly web crawler
python-pptx>=0.6.21  # PowerPoint file parsing
ebooklib>=0.18  # EPUB file parsing

# === Nihongo Mode — Japanese Teacher Plugin ===
pykakasi>=2.2.1  # furigana + romaji conversion
gtts>=2.5.0  # TTS fallback (no API key needed)
# openai-whisper>=20231117  # local whisper (optional, only if WHISPER_USE_API=false)

# Host machine tools (install via apt, not pip):
# sudo apt install xdotool wmctrl scrot xclip xdg-utils
# sudo apt install tesseract-ocr tesseract-ocr-eng
# sudo apt install ollama (for local LLM inference on RTX 3060)
