# Legion — cloud-first AI bot for desktop control

# Telegram bot
aiogram>=3.4.0,<4.0.0
python-dotenv>=1.0.0

# LLM — all cloud providers + tool calling
litellm>=1.40.0

# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# Vision / images
pillow>=10.0.0

# Async utilities
aiofiles>=23.2.0
aiohttp>=3.9.0

# System monitoring
psutil>=5.9.0

# ── Host machine tools (install separately, not pip) ──────────────────────
# sudo apt install xdotool wmctrl scrot xclip xdg-utils
# These enable: mouse/keyboard control, window management, screenshots, clipboard
