#!/usr/bin/env bash
# Wrapper to run genai-latex-proofreader from anywhere
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$DIR:$PYTHONPATH"
exec python3 -m genai_latex_proofreader.cli "$@"
