# Minimal dependencies for static typecheck CI (mypy workflow only)
# Keep this intentionally small to avoid runtime dependency conflicts.

aiogram>=3.4.1,<4.0.0
python-dotenv>=1.0.0
pydantic>=2
aiohttp>=3.9.0
httpx>=0.27.0
aiofiles>=23.2.0
requests>=2.31.0
PyYAML>=6.0
pytz>=2024.1
