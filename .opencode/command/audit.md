Perform a complete deep security and quality audit of the entire project. Check every file for: hardcoded secrets, SQL injection, unhandled exceptions, missing tests, performance bottlenecks, unused imports, and broken dependencies. Use @reviewer for analysis. Write full findings report to ~/swarm-bot/wiki/issues/audit-[today's date].md with severity ratings (CRITICAL/HIGH/MEDIUM/LOW) for each finding.