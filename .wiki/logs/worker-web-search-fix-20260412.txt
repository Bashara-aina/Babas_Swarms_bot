# Worker Web Search Fix — 2026-04-12

## Subtask 1: Fix `tools/web_search.py` ✅
- Wrapped synchronous `DDGS()` in `asyncio.to_thread()` for non-blocking execution
- Added `asyncio.wait_for()` with 8-second timeout
- Added retry with simplified query on timeout
- Proper error messages for timeout, empty results, ImportError, and general failures

## Subtask 2: Fix `core/self_awareness_gate.py` ✅
- Added patterns for "cari siapa [name]" → search for "{name} researcher"
- Added "site:linkedin.com OR site:researchgate.net OR site:scholar.google.com" suffix for person searches
- Proper query transformation tested and working

## Subtask 3: Fix `llm_client/__init__.py` lines 1255-1272 ✅
- Moved search trigger logic OUTSIDE the silent `try/except: pass`
- Actually call `get_search_trigger_message()` (logged)
- Instead of string concatenation, inject search results as a tool result message into the messages list
- Re-call LLM via `_call_model()` to synthesize a proper response with search results
- Proper fallback to raw append if synthesis fails
- Proper error handling with warnings

## Subtask 4: Test Suite ✅
- All 315 tests pass
- `asyncio.run(search_web('test query'))` completes without blocking

## Files Modified
1. `tools/web_search.py` — async + timeout + retry
2. `core/self_awareness_gate.py` — person search query patterns
3. `llm_client/__init__.py` — proper search result injection pipeline
