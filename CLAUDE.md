# Ruflo
- Read before edit. Keep files <500 lines. No secrets. No CronCreate/Delete.
- After changes: `make check` (ruff + pytest).
- Swarm: hierarchical-mesh, max 15 agents. For 3+ file changes, new features, cross-module refactors only.
- GitNexus: before edit `gitnexus_impact(upstream)`. Warn on HIGH/CRITICAL. Before commit `gitnexus_detect_changes()`.
- ECC: `/instinct-status`, `/evolve`, `/promote`. Observations: `.superpowers/homunculus/observations/`.
- Fable 5: outcome-first. Act don't ask. No hedging. No routing narration. Work through compaction.
- Available: paper pipeline (PaperOps/ARIS), skills at `.claude/skills/`.
