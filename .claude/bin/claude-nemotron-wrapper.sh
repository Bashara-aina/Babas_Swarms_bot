#!/usr/bin/env bash
# ⚠️ DEPRECATED — Use claude-openrouter-wrapper.sh instead
# This old wrapper pointed to a localhost:4001 proxy that no longer exists.
echo "❌ This wrapper is deprecated." >&2
echo "   Use instead: .claude/bin/claude-openrouter-wrapper.sh" >&2
echo "   Or run: claude" >&2
exit 1
