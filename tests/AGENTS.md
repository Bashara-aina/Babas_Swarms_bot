# Tests Directory Context
> Auto-generated by OpenCode elite setup | Updated: 2026-04-10

## Purpose
pytest-asyncio test suite for SwarmBot. Tests use the `auto` asyncio mode.

## Key Test Files
- `conftest.py` — pytest fixtures and configuration
- `test_agent_messaging.py` — Agent-to-agent messaging tests
- `test_jarvis_orchestrator.py` — Orchestrator tests
- `test_memory_manager.py` — Memory management tests
- `test_task_router.py` — Task routing tests
- `test_rate_limiter.py` — Rate limiting tests
- `test_security.py` — Security guard tests
- `test_health.py` — Health check tests

## Rules
- All tests use `pytest-asyncio` with `asyncio_mode = "auto"`
- Test path: `tests/` directory
- Python files: `test_*.py`
- Classes: `Test*`
- Functions: `test_*`
- Python path includes "." (root)

## Running Tests
```bash
pytest tests/ -x --asyncio-mode=auto -q
```

## Test Coverage
- Minimum coverage: 10%
- Excluded: tests/*, site-packages, setup.py