# Swarms Bot Directory Context
> Auto-generated by OpenCode elite setup | Updated: 2026-04-10

## Purpose
Enterprise-grade orchestration layer: routing, sessions, security, evaluation, and observability for the SwarmBot multi-agent system.

## Key Modules
- `agents/` — Agent registry and spawning
- `orchestrator/` — DAG executor, chief of staff, model router
- `routing/` — Cost routing, budget management
- `sessions/` — Session state management
- `security/` — Rate limiting, guard policies
- `evaluation/` — Quality evaluation and metrics
- `observability/` — Logging, cost metrics

## Rules
- All I/O operations must be async (asyncio/await)
- LLM calls go through llm_client.py only
- Security: never log API keys or secrets
- Session state persisted via aiosqlite
- Budget limits enforced before LLM calls

## Test Command
```bash
pytest tests/ -x --asyncio-mode=auto -q
```