# Handlers Directory Context
> Auto-generated by OpenCode elite setup | Updated: 2026-04-10

## Purpose
Contains 45+ aiogram router files that handle Telegram bot commands and message routing. Each file is a self-contained feature domain with its own router.

## Key Files
- `__init__.py` — Router registration order (critical: ai.router must be LAST)
- `ai.py` — NL catch-all handler, /run /think /agent /swarm commands
- `admin_handlers.py` — /budget /soul admin commands
- `brain.py` — /memories /briefing /learn /instincts
- `computer.py` — /do /screen /click /type /key /cmd /install (computer control)
- `system.py` — /start /stats /keys /models /git /maintenance /gpu
- `orchestrate.py` — /orchestrate /orchestrate_cancel
- `github_intel_handler.py` — /github_intel /eval_repo /upgrade_from

## Rules
- Router order matters — more specific handlers before general ones
- ai.router MUST be last (NL catch-all via F.text)
- overnight_handler before ai to avoid interception
- Voice router handles F.voice + F.audio + /voice_on/off/status/toggle
- NEVER edit __init__.py router order without understanding implications

## Test Command
```bash
pytest tests/ -x --asyncio-mode=auto -q
```